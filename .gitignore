# ignore all build directories
/build/**
# tmp contains temporary files
/tmp/**
# dist contains the binary build results
/dist/**
# build.properties store the user-specific properties 
# and should never be checked in or out.
# use build.properties.template as a template for 
# creating your own build.properties.
/build.properties
# sometimes junit4 produces temporary files that we do not want to check in
/junit*.*
# splash.png is generated by the build
/doc/manual/splash.png
/build/
/tmp/
