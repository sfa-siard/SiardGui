# See https://help.github.com/articles/dealing-with-line-endings/
# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.java text
*.properties text
*.js text
*.html text
*.css text
*.svg text
*.xml text
*.txt text
LICENSE text

# Declare files that will always have CRLF line endings on checkout.
# PowerShell scripts must always have CRLF endings
*.ps1 text eol=crlf
# cmd scripts must always have CRLF endings
*.cmd text eol=crlf

# Declare files that will always have LF line endings on checkout.
# MANIFEST may have any line endings but we prefer the same on all platforms
*.MF text eol=lf
# Same with .git* files
.git* text eol=lf
# shell scripts must always have LF endings
*.sh eol=lf

# Denote all files that are truly binary and should not be modified.
*.jar binary
*.png binary
*.jpg binary
*.gif binary
*.eot binary
*.ttf binary
*.woff binary
*.woff2 binary
*.pdf binary
*.db binary
*.siard binary
*.accdb binary
